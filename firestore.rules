rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Enable full read and write access for a shared, guest-accessible dashboard.
    // The application uses internal UI-level secure PIN code controls for roles.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
